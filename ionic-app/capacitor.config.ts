import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nautica.gym',
  appName: 'Nautica Gym',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
